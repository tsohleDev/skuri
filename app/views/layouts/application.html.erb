<!DOCTYPE html>
<html>
  <head>
    <title>Skuri</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body class='bg-quaternary min-h-screen' data-controller="mobile-menu">
    <%= render "shared/menu" %>

    <header class="h-[10vh] sm:h-[7vh] sticky top-0 flex justify-between items-center py-2 px-5 border-b border-primary shadow-b shadow-primary bg-quaternary z-10 md:relative" data-mobile-menu-target="header">
      <%= link_to "Skuri", root_path, class: "text-4xl font-bold" %>
      <%= render "shared/navbar" %>
    </header>

    <main class="min-h-[93vh]" data-mobile-menu-target="main">
      <%= render "shared/alerts" %>
      <%= yield %>
    </main>


    <footer class="sm:h-[13vh] bg-gray-800 text-white py-4" data-mobile-menu-target="footer">
      <div class="container h-full mx-auto text-center">
        <!-- Footer Content -->
        <p>&copy; 2014 Skuri. All rights reserved.</p>
        <p>Address | Phone | Email</p>

        <!-- Social Media Icons (replace with your own icons or links) -->
        <div class="flex justify-center space-x-4 mt-4">
          <a href="#" class="text-white hover:text-gray-400"><i class="fab fa-twitter"></i></a>
          <a href="#" class="text-white hover:text-gray-400"><i class="fab fa-facebook"></i></a>
          <a href="#" class="text-white hover:text-gray-400"><i class="fab fa-instagram"></i></a>
        </div>
      </div>
    </footer>
  </body>
</html>
