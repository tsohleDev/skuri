<!DOCTYPE html>
<html>
  <head>
    <title>Skuri</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body class"bg-quaternary">
    <header class="flex justify-between items-center py-2 px-5 border-b border-primary shadow-b shadow-primary bg-quaternary">
      <%= link_to "Skuri", root_path, class: "text-4xl font-bold" %>
       <%= render "layouts/navbar" %>
    </header>

    <main class="min-h-[90vh] bg-quaternary">
      <p class="text-green-500"><%= notice %></p>
      <p class="text-red-500"><%= alert %></p>
      <%= yield %>
    </main>

    <footer class="bg-gray-800 text-white py-4">
  <div class="container mx-auto text-center">
    <!-- Footer Content -->
    <p>&copy; <%= Time.current.year %> Skuri. All rights reserved.</p>
    <p>Address | Phone | Email</p>

    <!-- Social Media Icons (replace with your own icons or links) -->
    <div class="flex justify-center space-x-4 mt-4">
      <a href="#" class="text-white hover:text-gray-400"><i class="fab fa-twitter"></i></a>
      <a href="#" class="text-white hover:text-gray-400"><i class="fab fa-facebook"></i></a>
      <a href="#" class="text-white hover:text-gray-400"><i class="fab fa-instagram"></i></a>
    </div>
  </div>
</footer>
  </body>
</html>
