<div id="returnables" class="col-span-4 grid grid-cols-3 gap-8 bg-white p-4 rounded-lg shadow-lg">
    <% products.each do |product| %>
        <%= render 'users/item', item: product, returnable: true %>
    <% end %>
</div>